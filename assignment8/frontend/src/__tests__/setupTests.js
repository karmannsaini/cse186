// Common test artefacts: import this file in test suites that need shared
// helpers (e.g. custom render, shared mocks). The root frontend/setupTests.js
// is loaded by vite.config.js for all tests (expect matchers, cleanup).
